spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none
  sendgrid:
    apiKey: ${SENDGRID_API_KEY}
    from-email: ${SENDGRID_FROM_EMAIL}
    template-id: ${SENDGRID_TEMPLATE_ID}
    verification-link: ${SENDGRID_VERIFICATION_LINK}

# application-dev.yml
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
openapi:
  service:
    api-docs: backend-service
    server: http://localhost:${server.port}
    title: Backend service
    version: 1.0.0

jwt:
  secretKey: abcd312
  expiryMinutes: 5
  expiryDays: 14
  accessKey: TZTTPUsuYPp3E2st1zpO3X223a5tgSblqDaBPJGnD/Y=
  refreshKey: 7TjZb0X7RmqfKXUJ5lU2FV2VaS2pNh3EkdOhLuJSFTg=
